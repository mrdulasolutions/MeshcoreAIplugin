# MeshCore AI plugin

Read `SKILL.md` first. That file is the agent procedure.

Run `python3 scripts/meshcore_ai.py --help`.

Rules:

- Do not take BLE from the official MeshCore app.
- Use `meshcore://` deep links and the app sqlite DB.
- `post` is local history only; LoRa still goes through the app.
